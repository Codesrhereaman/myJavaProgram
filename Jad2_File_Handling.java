import java.io.File;
import java.io.FileWriter;
import java.io.IOException;
import java.util.Scanner;

public class Jad2_File_Handling {
    public static void main(String[] args)  {

        //creating a file
/*
        File myfile = new File("firstFile.txt");
        try {
            myfile.createNewFile();
        } catch (IOException e) {
           e.toString();
        }

        //write to  a file
        try {
            FileWriter myfile = new FileWriter("firstFile.txt");
            myfile.write("hello guys i have created a new file using java\n hurrah!!");
            myfile.close();
        } catch (IOException e) {
            throw new RuntimeException(e);
        }


        File myfile = new File("firstFile.txt");
        try {
            Scanner sc = new Scanner(myfile);
            while(sc.hasNextLine()){
                String line = sc.nextLine();
                System.out.println(line);
            }
            sc.close();
        } catch (IOException e) {
            throw new RuntimeException(e);
        }


        File myfile = new File("firstFile.txt");
        if(myfile.delete()){
            System.out.println("the file "+myfile.getName()+" has been deleted");
        }
        else{
            System.out.println("error");
        }

 */




    }
}
